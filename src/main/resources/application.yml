# HTTP (Tomcat) port
server:
  port: 8763

#Application name
spring:
  application:
    name: micro-client

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://peer1:8761/eureka/,http://localhost:8862/eureka/

# application parameters
#app:
#  service-url: http://MICRO-SERVICE/

